import Keyboard
import Tonic
import SwiftUI
import PlaygroundSupport

struct ContentView: View {
    var body: some View {
        Keyboard()
    }
}

PlaygroundPage.current.setLiveView(ContentView())
