import SwiftUI
import Keyboard

struct ContentView: View {
    var body: some View {
        Keyboard()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
